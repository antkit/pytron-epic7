export enum Paths {
  Root = ".pytron-epic7",

  PyEnv = "pyenv",
  Res = "res",
}

export enum Configs {
  ChannelEnv = "env",
  ChannelRun = "run",
  ChannelPytron = "pytron",
  ConfigFile = "config.json",
}
